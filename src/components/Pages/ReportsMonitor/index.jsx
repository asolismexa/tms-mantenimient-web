import { Box } from '@mui/material'
import { DataGrid } from '@mui/x-data-grid'

export function ReportsMonitor () {
  return (
    <Box>
      <DataGrid
        columns={[]}
        rows={[]} />
    </Box>
  )
}
